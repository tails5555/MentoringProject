<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
             "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="net.skhu.mentoring.mapper.NoticeBBSMapper">

  <select id="findAll" resultType="NoticeBBS">
    select * from NoticeBBS
  </select>
  <select id="findOne" resultType="NoticeBBS">
    select * from NoticeBBS WHERE id=#{id}
  </select>
</mapper>
